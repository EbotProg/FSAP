<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/download.css">
    <link rel="stylesheet" href="/styles/all.css">

</head>
<body>

    <div class="login-main">
        <div class="login-container container">
            <div class="login-image">
                <svg xmlns="http://www.w3.org/2000/svg" width="70" height="70" fill="rgba(48, 3, 48, 0.395)" class="bi bi-files" viewBox="0 0 16 16">
                    <path d="M13 0H6a2 2 0 0 0-2 2 2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h7a2 2 0 0 0 2-2 2 2 0 0 0 2-2V2a2 2 0 0 0-2-2zm0 13V4a2 2 0 0 0-2-2H5a1 1 0 0 1 1-1h7a1 1 0 0 1 1 1v10a1 1 0 0 1-1 1zM3 4a1 1 0 0 1 1-1h7a1 1 0 0 1 1 1v10a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V4z"/>
                  </svg>
                  <h1 class="login-title2">FSAP</h1>
            </div>
            <div class="login-content">
                <div class="login-content-header d-flex flex-column align-items-center">
                    <h2 class="font-weight-bold login-title1">Download</h2>
                    <div class="login-border mb-3"></div>
        <% if(locals.fileNotFound == true){ %>
            <p class="alert alert-danger">Sorry!File was either deleted or doesn't exist</p>
        <% } %>
        <% if(locals.err){ %>
            <p class="alert alert-danger">Incorrect password</p>
        <% } %>
        <% if(locals.passwordIsInvalid == true){ %>
            <p class="alert alert-danger">Invalid password</p>
        <% } %>
      
    </div>
        <form method="post">
            <div class="row gy-3 gx-1">
                <% if(locals.noPassword == false){ %>
                    <div class="form-floating">
                        <input type="password" class="form-control" name="password" id="password" placeholder="password" >
                        <label for="password" class="align-self-start">Password</label>
                    </div>
                <% } %>
                
              
               <div class="form-group">
                <button type="submit" class="btn w-100 bottom-btn">Download</button>

               </div>
            </div>
            
            
        </form>
        <% if(locals.authenticatedUser == false){ %>
            <p class="py-2 bottom-msg">Do you want to know more about FSAP?<a class="link-msg" href="/">Visit our site</a></p>
        <% } %>
        <% if(locals.authenticatedUser == true){ %>
            <p class="py-2 bottom-msg"><a class="link-msg" href="/welcome">return to FSAP</a></p>
            <% } %>
       
</div>
</div>

<%- include('footer') -%> 